server:
    port: 8080
spring:
    application.name: gateway
    cloud:
        config:
            uri: http://localhost:1111
            fail-fast: true
            password: 12345
        gateway.discovery.locator.enabled: true