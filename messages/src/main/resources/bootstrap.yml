spring:
    application.name: messages
    cloud:
        config:
            password: 12345
            discovery.enabled: true
eureka:
    client:
        enabled: true
        serviceUrl.defaultZone: http://localhost:1111/eureka/